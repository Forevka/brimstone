[package]
name = "brimstone_serialized_heap"
version.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
brimstone_core = { workspace = true, default-features = false, features = ["no_jemalloc"] }

[build-dependencies]
brimstone_core = { workspace = true, default-features = false, features = ["no_jemalloc"] }

[features]
default = []
no_jemalloc = ["brimstone_core/no_jemalloc"]